# Classical ML only -- no deep learning frameworks.
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1
pytest==8.3.3

# Resampling strategies compared in notebook 04. Built on scikit-learn, so
# still classical ML -- it adds SMOTE and the over/under-samplers, not a
# modelling framework.
imbalanced-learn==0.14.2
